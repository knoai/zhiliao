# JWT 配置
JWT_SECRET=zhiliao-secret-key-for-development-only
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=168

# 本地 SQLite 配置（无需 Docker）
DATABASE_URL=sqlite+aiosqlite:///./cloud_notes.db

# 本地 PostgreSQL 配置 (Docker)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres123@localhost:5432/cloud_notes

# 远程 PostgreSQL 配置（备用）
# DATABASE_URL=postgresql+asyncpg://user:user123@14.103.155.159:31543/mydb

# 应用配置
DEBUG=True
FRONTEND_URL=http://localhost:5174
